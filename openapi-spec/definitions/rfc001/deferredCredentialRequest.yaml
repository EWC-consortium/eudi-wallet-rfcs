title: Deferred Credential Request
type: object
properties:
  transaction_id:
    type: string
    description: >
      REQUIRED. String identifying a Deferred Issuance transaction. 
      The Credential Issuer MUST invalidate the transaction_id after the Credential for which 
      it was meant has been obtained by the Wallet.
required:
  - transaction_id
